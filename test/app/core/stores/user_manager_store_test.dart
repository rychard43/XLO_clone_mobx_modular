import 'package:flutter_modular/flutter_modular_test.dart';
import 'package:flutter_test/flutter_test.dart';

import 'package:xlo_mobx_parse/app/core/stores/user_manager_store.dart';
import 'package:xlo_mobx_parse/app/app_module.dart';

void main() {
  initModule(AppModule());
  // UserManagerStore usermanager;
  //
  setUp(() {
    //     usermanager = AppModule.to.get<UserManagerStore>();
  });

  group('UserManagerStore Test', () {
    //   test("First Test", () {
    //     expect(usermanager, isInstanceOf<UserManagerStore>());
    //   });

    //   test("Set Value", () {
    //     expect(usermanager.value, equals(0));
    //     usermanager.increment();
    //     expect(usermanager.value, equals(1));
    //   });
  });
}
